using Photon.Pun; // Ensure you have Photon.Pun namespace for PhotonView
using UnityEngine;

public class Photon_Manager_View : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
    }
}
